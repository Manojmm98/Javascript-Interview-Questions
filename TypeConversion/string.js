
// string conversion 

// check the type

let str = 'Manoj';
console.log(typeof str); // ans is string

str= '10'

console.log(typeof str);

// if we want to change the string to a Number or any type then we have to do it by below

let newstr = Number(str); // now 10 become a number

console.log( newstr);

// find the type of below

console.log(typeof typeof typeof typeof typeof typeof typeof typeof typeof typeof true);
// answer will be string because last type of return boolean as string and after that all typeof take it as string