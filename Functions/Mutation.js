

let obj ={
    name : 'manoj',

    location : 'bbsr'
}

console.log(obj);

// we have changed the name and  location from one to difrrent this is called mutation
obj.location ='hyderabad'

obj.name = 'raja'

console.log(obj);
